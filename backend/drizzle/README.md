# Drizzle migrations

هذا المجلد يحتوي على هجرات Drizzle المُولَّدة تلقائياً (`drizzle-kit generate`).

- **لا تقسّم ملفات `*.sql`** إلى ملفات أصغر؛ كل ملف هجرة يُطبَّق كنسخة واحدة.
- التحذيرات الخاصة بحجم الملف (مثل "Split or Die") يمكن تجاهلها لهذه الملفات.
