{
  "version": "1.0",
  "nodes": [
    {
      "id": "get-vouchers-list",
      "workflowId": "backend/vouchers",
      "title": "Get Vouchers List",
      "description": "GET /api/businesses/:bizId/vouchers with optional query type=receipt|payment|transfer. Returns array of vouchers for business ordered by createdAt desc. Requires Authorization header and bizId membership. Returns 200 with [{id, voucherType, voucherNumber, amount, fromAccountId, toAccountId, categoryId, status, description, voucherDate, createdAt, createdBy}]. Returns 401 if unauthorized. Returns 403 if not member of business.",
      "nodeType": "feature",
      "fileName": "get-vouchers-list",
      "position": { "x": 100, "y": 100 },
      "dependencies": [],
      "testLayers": ["api"]
    },
    {
      "id": "post-voucher-create-receipt",
      "workflowId": "backend/vouchers",
      "title": "Post Voucher Create Receipt",
      "description": "POST /api/businesses/:bizId/vouchers with body {voucherType: 'receipt', amount, toAccountId, fromAccountId, categoryId, description, voucherDate}. Validates amount > 0 and accounts belong to business. Generates sequential voucherNumber. Creates voucher record and journal entry lines (debit toAccount, credit fromAccount). Updates account balances. Returns 201 with created voucher {id, voucherNumber, ...}. Returns 400 if amount invalid or accounts missing. Returns 403 if accounts not in business. Requires vouchers create permission.",
      "nodeType": "feature",
      "fileName": "post-voucher-create-receipt",
      "position": { "x": 300, "y": 100 },
      "dependencies": [],
      "testLayers": ["api"]
    },
    {
      "id": "post-voucher-create-payment",
      "workflowId": "backend/vouchers",
      "title": "Post Voucher Create Payment",
      "description": "POST /api/businesses/:bizId/vouchers with body {voucherType: 'payment', amount, toAccountId, fromAccountId, categoryId, description, voucherDate}. Same validation as receipt. Debits fromAccountId and credits toAccountId. Creates voucher and journal lines. Updates balances. Returns 201 with voucher. Returns 400 if validation fails. Requires vouchers create permission.",
      "nodeType": "feature",
      "fileName": "post-voucher-create-payment",
      "position": { "x": 500, "y": 100 },
      "dependencies": [],
      "testLayers": ["api"]
    },
    {
      "id": "post-voucher-create-transfer",
      "workflowId": "backend/vouchers",
      "title": "Post Voucher Create Transfer",
      "description": "POST /api/businesses/:bizId/vouchers with body {voucherType: 'transfer', amount, toAccountId, fromAccountId, description, voucherDate}. Transfers amount between two accounts within same business. Creates voucher and journal lines. Returns 201 with voucher. Returns 400 if validation fails.",
      "nodeType": "feature",
      "fileName": "post-voucher-create-transfer",
      "position": { "x": 700, "y": 100 },
      "dependencies": [],
      "testLayers": ["api"]
    },
    {
      "id": "post-voucher-status-cancelled",
      "workflowId": "backend/vouchers",
      "title": "Post Voucher Status Cancelled",
      "description": "POST /api/businesses/:bizId/vouchers/:id/status with body {status: 'cancelled'}. For confirmed vouchers, delegates cancellation to transaction engine and reverts balances without creating any compensating voucher document. Returns 200 with updated voucher, 400 for invalid transitions.",
      "nodeType": "feature",
      "fileName": "post-voucher-status-cancelled",
      "position": { "x": 300, "y": 300 },
      "dependencies": ["get-vouchers-list"],
      "testLayers": ["api"]
    },
    {
      "id": "delete-voucher",
      "workflowId": "backend/vouchers",
      "title": "Delete Voucher",
      "description": "DELETE /api/businesses/:bizId/vouchers/:id. Validates ownership and delegates to cancellation behavior in transaction engine. Returns 200 with {success: true}. Returns 404 if not found.",
      "nodeType": "feature",
      "fileName": "delete-voucher",
      "position": { "x": 100, "y": 300 },
      "dependencies": [],
      "testLayers": ["api"]
    },
    {
      "id": "get-voucher-transitions",
      "workflowId": "backend/vouchers",
      "title": "Get Voucher Transitions",
      "description": "GET /api/businesses/:bizId/vouchers/:voucherId/transitions. Returns array of available workflow transitions for voucher based on current status and operation type. Returns 200 with [{id, fromStatus, toStatus, label, requiresApproval}]. Empty array if no transitions available. Used for workflow state machine.",
      "nodeType": "feature",
      "fileName": "get-voucher-transitions",
      "position": { "x": 500, "y": 300 },
      "dependencies": ["get-vouchers-list"],
      "testLayers": ["api"]
    },
    {
      "id": "post-execute-transition",
      "workflowId": "backend/vouchers",
      "title": "Post Execute Transition",
      "description": "POST /api/businesses/:bizId/vouchers/:voucherId/transition with body {transitionId, note}. Executes workflow transition. Updates voucher status. Logs transition in workflow_history. Returns 200 with updated voucher and new status. Returns 404 if voucher or transition not found. Returns 400 if transition not allowed from current status. Requires workflow execute permission.",
      "nodeType": "feature",
      "fileName": "post-execute-transition",
      "position": { "x": 700, "y": 300 },
      "dependencies": ["get-voucher-transitions"],
      "testLayers": ["api"]
    }
  ],
  "edges": [
    { "id": "list-to-cancelled-status", "source": "get-vouchers-list", "target": "post-voucher-status-cancelled" },
    { "id": "list-to-transitions", "source": "get-vouchers-list", "target": "get-voucher-transitions" },
    { "id": "transitions-to-execute", "source": "get-voucher-transitions", "target": "post-execute-transition" }
  ]
}
